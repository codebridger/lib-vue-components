import { describe, it, expect } from "vitest"; import { mount } from "@vue/test-utils"; import Card from "../Card.vue"; describe("Card Component", () => { const createWrapper = (props = {}, slots = {}) => { return mount(Card, { props, slots }); }; describe("Rendering", () => { it("renders as div element by default", () => { const wrapper = createWrapper(); expect(wrapper.find("div").exists()).toBe(true); }); it("renders with default classes", () => { const wrapper = createWrapper(); expect(wrapper.classes()).toContain("p-4"); expect(wrapper.classes()).toContain("shadow-[4px_6px_10px_-3px_#bfc9d4]"); expect(wrapper.classes()).toContain("rounded"); expect(wrapper.classes()).toContain("border"); }); it("renders with custom class", () => { const wrapper = createWrapper({ class: "custom-card" }); expect(wrapper.classes()).toContain("custom-card"); }); }); describe("Props and Styling", () => { it("applies disabled styling when disabled is true", () => { const wrapper = createWrapper({ disabled: true }); expect(wrapper.classes()).toContain("opacity-50"); expect(wrapper.classes()).toContain("cursor-not-allowed"); }); it("does not apply disabled styling when disabled is false", () => { const wrapper = createWrapper({ disabled: false }); expect(wrapper.classes()).not.toContain("opacity-50"); expect(wrapper.classes()).not.toContain("cursor-not-allowed"); }); it("applies bg-white when no bg color class is present", () => { const wrapper = createWrapper(); expect(wrapper.classes()).toContain("bg-white"); }); it("does not apply bg-white when bg color class is present", () => { const wrapper = createWrapper({ class: "bg-blue-500" }); expect(wrapper.classes()).toContain("bg-blue-500"); expect(wrapper.classes()).not.toContain("bg-white"); }); }); describe("Slot Rendering", () => { it("renders default slot content", () => { const wrapper = createWrapper({}, { default: "<div class=\"card-content\">Card content</div>" }); const content = wrapper.find(".card-content"); expect(content.exists()).toBe(true); expect(content.text()).toContain("Card content"); }); }); describe("Edge Cases", () => { it("handles undefined props gracefully", () => { const wrapper = createWrapper({}); expect(wrapper.exists()).toBe(true); expect(wrapper.classes()).toContain("p-4"); }); it("handles empty slots", () => { const wrapper = createWrapper({}); expect(wrapper.exists()).toBe(true); expect(wrapper.classes()).toContain("p-4"); }); }); });
