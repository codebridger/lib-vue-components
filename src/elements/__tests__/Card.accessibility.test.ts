import { describe, it, expect } from "vitest"; import { mount } from "@vue/test-utils"; import Card from "../Card.vue"; describe("Card Component Accessibility", () => { const createWrapper = (props = {}, slots = {}) => { return mount(Card, { props, slots }); }; describe("Disabled State Accessibility", () => { it("applies disabled styling for visual indication", () => { const wrapper = createWrapper({ disabled: true }); expect(wrapper.classes()).toContain("opacity-50"); expect(wrapper.classes()).toContain("cursor-not-allowed"); }); it("does not apply disabled styling when disabled is false", () => { const wrapper = createWrapper({ disabled: false }); expect(wrapper.classes()).not.toContain("opacity-50"); expect(wrapper.classes()).not.toContain("cursor-not-allowed"); }); }); describe("Focus Management", () => { it("supports focus event handling", async () => { const wrapper = createWrapper(); await wrapper.trigger("focus"); expect(wrapper.emitted("focus")).toBeTruthy(); }); it("supports blur event handling", async () => { const wrapper = createWrapper(); await wrapper.trigger("blur"); expect(wrapper.emitted("blur")).toBeTruthy(); }); }); describe("Screen Reader Support", () => { it("provides meaningful content structure", () => { const wrapper = createWrapper({}, { default: "<p>Product description here</p>" }); expect(wrapper.text()).toContain("Product description here"); }); it("announces disabled state visually", () => { const wrapper = createWrapper({ disabled: true }); expect(wrapper.classes()).toContain("opacity-50"); }); }); });
